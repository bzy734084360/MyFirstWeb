@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <title>后台Admin</title>
    <script src="~/Scripts/jquery-1.10.2.js"></script>
    @*<script src="~/Content/layui/layui.js"></script>*@
    <script src="~/Content/layui/layui.all.js"></script>
    <link href="~/Content/layui/css/layui.css" rel="stylesheet" />
</head>
<body>
    <div><span><a onclick="OutLogin()">退出登录</a></span></div>
    <div>
        @RenderBody()
    </div>
    @*<div>
            <button onclick="ShowMsg()">展示母版页弹窗</button>
            <button onclick="ShowMymod()">展示母版页拓展模块</button>
        </div>*@

    <script>
        ///**
        //扩展一个test模块 /Scripts/mymod.js
        //**/
        //layui.extend({
        //    mymod: '{/}/Scripts/mymod' // {/}的意思即代表采用自有路径，即不跟随 base 路径
        //})
        //layui.use(['layer', 'form', 'mymod'], function () {
        //    var layer = layui.layer
        //    , form = layui.form
        //    , mymod = layui.mymod;
        //    //mymod.hello('拓展Layui模块!');
        //});
        //function ShowMsg() {
        //    layer.msg("母版页提示框", { icon: 1, time: 1500 });
        //}
        //function ShowMymod() {
        //    //全模块加载无法使用： 自定义模块
        //    mymod.hello('拓展Layui模块!');
        //}
        function OutLogin() {
            $.ajax({
                type: 'post',
                dataType: "text",
                url: "../FormStudy/OutLogin",
                data: "",
                cache: false,
                async: false,
                success: function (data) {
                    window.location.href = '../Home/Index';
                },
                error: function (data) {
                    alert("error:" + JSON.stringify(data));
                }
            });
        }
    </script>
</body>
</html>
